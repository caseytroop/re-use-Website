use greengrp;

create table store
	(
	ID			int,
	name		varchar(20),
	goods		varchar(15),
	lat			double,
	lon			double,
	address		varchar(40),
	phone		varchar(15),
	email		varchar(25),
	img			varchar(20)
	);
