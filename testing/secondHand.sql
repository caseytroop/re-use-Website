use greengrp;

create table store
	(
	ID			int,
	name		varchar(35),
	lat			double,
	lon			double,
	address		varchar(50),
	phone		varchar(15),
	email		varchar(40),
	img			varchar(20),
	goods		varchar(20)
	);
